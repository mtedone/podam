
machine:
  timezone:
    America/Los_Angeles
  java:
    version: oraclejdk8
    
test:    
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/PodamTests/
    - find . -type f -regex ".*/src/test/resources/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/PodamTests/ \;
