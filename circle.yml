machine:
  ruby:
version: rbx-2.2.6
test:
  override:
    #as a ruby machine lets see if there is no need to do maven commands
    #mvn clean verify
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
