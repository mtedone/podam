machine:
  java:
   version: openjdk7
  
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/testmeta/
    - find . -type f -regex ".*/test/resources/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/testmeta/ \;
    
general:
  artifacts:
    - "test" # relative to the build directory
    
 
  
