machine:
  java:
   version: openjdk7
  
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/testfile.xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
    
general:
  artifacts:
    - "test" # relative to the build directory
    
