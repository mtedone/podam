machine:
  java:
   version: openjdk7
  
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/test/
    - find . -type f -regex ".*/resources.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/test/ \;
    
general:
  artifacts:
    - "test" # relative to the build directory
    
 
  
